# 项目协作协议

本文件以及同目录的其他 md 文档都只是起点，不是定稿：请按项目实际情况增删章节、改写措辞，把它们变成该项目的专用文档。文件名也可调整，改后保持互相引用一致。

默认优先级（高到低）：文末底线 > 用户当场指令 > `CONSTRAINTS.md` > `AGENTS.md` > `TASK.md`。这条链和下列规则都可由项目改写（文末底线除外）；未规定的事项由执行者自行判断，除破坏性操作须先确认外不必请示。

## 工作方式

- 中文沟通；结论以已验证事实为准，无法验证时写 `[待核实]`。
- 开始或续接任务时先读本目录的任务与约束文档（默认 `TASK.md`、`CONSTRAINTS.md`、`handoff.md`），用一句话复述目标与边界。
- 本目录的 md 若还是通用模板，先按项目实际信息改写、补全（项目档案、红线、任务目标、版本规则），再开始任务；无法确定的部分保持占位并问用户。
- 报错或异常先查证据再改，不凭猜测下结论。
- 不泄露凭证与个人信息；不伪造测试结果、来源、版本号或发布状态。

## 文件与代码

- 只改完成任务所必需的文件；不做无关格式化、重排或重命名。
- 删除、覆盖、迁移、批量改写、权限调整或对外写入前，先说明精确目标与影响范围；未经授权不执行。
- 测试用夹具、临时目录或只读检查，不触碰源数据，不跑生产任务。
- 改完跑最小必要验证；未跑或未过不得宣称成功。

## 版本与 Git

- 版本号与构建标记集中在一处维护（默认 `VERSION.md`），其他文件只引用。
- 发布完成的判据：测试通过 + 目标发布物已实际替换 + 版本与 SHA-256 已核对；候选产物不算已发布。
- 可自动 `git add` + `git commit`；`push`、重写历史、清理工作区须单独授权。

## 对话记录

- 每轮对话结束前写一份记录并保存，不必等用户要求；同一分支的记录追加到同一文件。
- 路径：`对话记录/<当前 Git 分支名>.md`；不是 Git 仓库或取不到分支名时，用目录名代替。
- 每条记录写：时间、目标、改动（文件与原因）、验证结果、未完成项。
- 记录是按对话累积的过程档案，不替代 `handoff.md` 的当前状态。

## 交付

- 说明改动、验证结果、未完成项与 Git 状态；有发布物时附版本与 SHA-256。
- 结束前把同样内容更新到 `handoff.md`，别让交接文档停在空模板。

## 底线（不可改写）

以上任何改写都不得放宽：不伪造结果、不泄露凭证、不触碰 `CONSTRAINTS.md` 标明的只读数据。
